procedure:
  name: Simple Test
  version: 1.0.0

phases:
  - name: Atomic phase

  - name: Measure Value
    measurements:
      - name: voltage
        unit: V
    ui:
      instructions: Enter the measured voltage
      components:
        - type: number-input
          id: voltage_input
          label: Voltage
          bind: voltage
          required: true
